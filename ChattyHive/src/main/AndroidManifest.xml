<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chattyhive.chattyhive"
    android:versionCode="1"
    android:versionName="0.1.5" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launcher_launcher_b"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--
        activity
            android:name="com.chattyhive.chattyhive.Home_old"
            android:description="@string/hello_world"
            android:label="@string/app_name"
            android:logo="@drawable/launcher_launcher_a"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity
        -->
        <activity
            android:name="com.chattyhive.chattyhive.LoginActivity_old"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>

        <!--
            <service
            android:name="com.chattyhive.chattyhive.backgroundservice.CHService"
            android:enabled="true"
            android:exported="true" >
        </service>

        <receiver android:name="com.chattyhive.chattyhive.backgroundservice.CHServiceLauncher" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver android:name="com.chattyhive.chattyhive.backgroundservice.CHServiceNetworkListener" >
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        -->

        <activity
            android:name="com.chattyhive.chattyhive.Main"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.WithNoActionBar"
            android:windowSoftInputMode="adjustResize|stateUnchanged" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.chattyhive.chattyhive.Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.WithNoActionBar"
            android:windowSoftInputMode="adjustResize|stateUnchanged">
        </activity>
    </application>

</manifest>